// métodos comuns de strings

const email = 'ikamusume@gmail.com'

const lastIndexOfA = email.lastIndexOf('k')

console.log(lastIndexOfA)

const emailSlice = email.slice(3,9)

console.log(emailSlice)

const emailReplace = email.replace('ikamusume', 'manakalaala')

console.log(emailReplace)